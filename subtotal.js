var basket = require('./basket');
var customers = require('./customers');

var subtotal = {
  calc: function(){
    // for each item in basket, iterate through and add prices up
    // afterwards, if bogof, half the cost
    // then check if customer has discount, remove 5%
  }
}